version: '3.8'

services:
  tritone:
    build: .
    ports:
      - "3000:3000"
    volumes:
      # Persist data directory
      - ./data:/app/data
    environment:
      - NODE_ENV=production
    restart: unless-stopped
